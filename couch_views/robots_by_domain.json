{
   "_id": "_design/robotstxt",
   "language": "javascript",
   "views": {
       "by_domain": {
           "map": "function(doc) { if(doc.doc_type == 'robotstxt') { emit([doc.protocol, doc.domain], doc._id); }  }"
       }
   }
}
